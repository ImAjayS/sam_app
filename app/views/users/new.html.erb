<% provide(:title, 'Sign up') %>
<h1>Sign Up Mate and come aboard</h1>

<div class="row">
  <div class="col-md-4 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f:f  %>
      <%= f.submit "Create my account", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
